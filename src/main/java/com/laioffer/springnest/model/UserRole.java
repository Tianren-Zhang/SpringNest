package com.laioffer.springnest.model;

// enum (short for "enumeration") is a special data type that represents a group of constants
// has Strong Type Safety, Readability, and Integration with Authorization
public enum UserRole {
    ROLE_HOST,
    ROLE_GUEST
}